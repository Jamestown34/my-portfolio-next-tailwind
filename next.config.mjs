// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // If you plan to use external images, configure them here.
    // For now, all images are assumed to be in the /public directory.
    // domains: ['example.com'], // Uncomment and add your domain if needed
  },
};

export default nextConfig;
